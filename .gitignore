# Python
__pycache__/
*.pyc
.venv/
venv/

# Build output — regenerated by CI on every run and deployed straight to
# Pages via the upload-pages-artifact step, so it does not need to be committed.
public/
